{
  "name": "zyntiq_cert",
  "version": "1.0.0",
  "description": "Root project manager for Zyntiq Cert",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client",
    "dev": "npx concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:network": "npx concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client -- --host\"",
    "start": "npm run dev"
  }
}
